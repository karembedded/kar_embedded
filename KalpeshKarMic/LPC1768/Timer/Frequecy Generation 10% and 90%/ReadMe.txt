In first Board to generate a Timer Frequecy using Timer_Match.c File.
and 2nd  Board To capture the Frequency using the Timer_Capture.c File.
for reffer the Frequency_Generation.png image.

Setting:
flash->Configure flash tool->utilities->untick use debug driver
					set the Coocox debugger.
flash->Configure flash tool->debug->select the use->set the coocox debugger>setting->set the Adaptor : NGX-USB-JTAG

